const profile = require('express').Router();

// Forwarding from /api/profile

// This controller will handle all profile related requests

profile.get('/', (req, res) => {

})

module.exports = profile;


